// test_download.js - removed per user request
// Previously this script invoked the /api/certificate/download endpoint
// and saved the returned image locally. The user requested deletion.
// If you need to reinstate a test harness, recreate a small script that
// fetches /api/certificate/download?serial=<serial> and writes the response to a file.

console.log('test_download.js intentionally disabled');
